## Banco de Dados

Arquivo de inicialização sugerido (execute no MySQL): `schema.sql`

Configure `.env` com:

```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=senha
DB_NAME=psicocare
JWT_SECRET=supersecret
JWT_EXPIRES_IN=1d
```






